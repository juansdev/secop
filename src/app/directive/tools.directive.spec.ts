import { ToolsDirective } from './tools.directive';

describe('ToolsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolsDirective();
    expect(directive).toBeTruthy();
  });
});
